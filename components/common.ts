import styled from "@emotion/styled";

export const EmptyButton = styled.button`
  padding: 0;
  margin: 0;
  border: none;
  background: none;
  color: inherit;
  font: inherit;
  cursor: pointer;
  appearance: none;
`;
