/** @jsx jsx */
import { css, jsx } from "@emotion/core";

import Navbar from "~/components/navbar";

const Home = () => (
  <>
    <Navbar />
    <main>dig dig a hole</main>
  </>
);

export default Home;
